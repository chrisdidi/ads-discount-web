const cartStyles = {
  cartButtonStyle:
    "fixed  mb-3 mr-3 bg-yellow-600 w-14 h-14 flex items-center justify-center rounded-full shadow-md absolute bottom-0 right-0 cursor-pointer hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1",
  cartContainer:
    "w-full fixed bottom-0 right-0 h-screen bg-gray-50 transition-all duration-300",
};

export default cartStyles;
